import React, {Component} from 'react';
import './saved.css'

class Saved extends Component {
    render(){
        return(
            <div className="saved">Saved Results</div>
        )
    }
}

export default Saved;